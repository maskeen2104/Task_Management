{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": ["-std=c++11", "-g", "${file}", "${workspaceFolder}/src/task.cpp", "-o", "${fileDirname}/output", "-arch", "x86_64"],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
