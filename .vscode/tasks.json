{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-std=c++11",
                "-g",
                "${workspaceFolder}/src/main.cpp",
                "${workspaceFolder}/src/task.cpp",
                "-o",
                "${workspaceFolder}/src/output",
                "-arch",
                "x86_64"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}

